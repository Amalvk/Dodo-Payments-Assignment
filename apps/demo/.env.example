# Copy to .env.production (or set in your host's build settings) before building
# for deployment. Points the SDK at wherever apps/checkout is actually hosted —
# without this, a production build still defaults to http://localhost:5174.
VITE_CHECKOUT_URL=https://your-checkout-deployment.example.com
